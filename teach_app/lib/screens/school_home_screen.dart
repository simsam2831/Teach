import 'package:flutter/material.dart';

class SchoolHomeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('School Home'),
      ),
      // Contenu de l'écran pour les écoles
    );
  }
}
